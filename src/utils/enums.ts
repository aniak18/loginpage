export enum ValidationMessages {
  CONFIRM_PWD_MESSAGE = "Confirm password is not matching password",
  PWD_VALIDATIONS = "Minimum 8 characters, at least one letter, one number and one special character",
  PHONE_NUMBER = "Please enter a valid 10-digit phone number",
}

export enum statusCodes {
  Success = 200,
  NoContent = 204,
  Unauthorized = 401,
}
